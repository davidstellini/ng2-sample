import { Component } from '@angular/core';

@Component({
  selector: 'section-manager',
  // styleUrls: [ './app.style.css' ],
  template: require('./sectionManager.component.html')
})
export class SectionManagerComponent {
  test: string = 'Hello from NG2!';
}
